# Ralph Progress Log
Started: Wed Feb 11 02:33:27 +08 2026
---

## Codebase Patterns
- OpportunityScan.tsx contains both `OpportunityScan` and `KatoQueue` components plus all SVG icons
- Opportunity interface extended with: `category`, `source`, `status`, `actedAt`, `dismissedAt` fields
- Opportunity types now include: blocker, ready, opportunity, suggestion, deadline, idea, system
- Source badges use `getSourceColor()` helper — ROADMAP=brown, Project=warning, External=purple, System=green
- Active/Archive toggle uses local `actionState` (Record<string, 'acted'|'dismissed'>) — future: persist to backend
- Sorting: ROADMAP/P0 items first, then blockers, then by priority
- All SVG icons are defined as inline function components at the bottom of OpportunityScan.tsx
- **Wabi-Sabi theme** (v4): warm beige/granola light theme, NOT dark
- CSS variables in index.css: `--bg-primary: #FAF9F7`, `--bg-secondary: #FFFFFF`, `--accent-primary: #8B7355`
- Colors use CSS variables via inline `style={{}}` props, NOT Tailwind color classes
- Card bg: `var(--bg-secondary)` (white), section bg: `var(--bg-tertiary)`, muted bg: `var(--bg-muted)`
- Borders use `var(--border-subtle)`, `var(--border-medium)`, `var(--border-strong)`
- Shadows: `var(--shadow-sm)`, `var(--shadow-md)`, `var(--shadow-lg)`
- Semantic colors: success=#7A9E7E, warning=#C9A959, error=#B87A7A, primary=#8B7355
- Agent color mapping: kato=#8B7355, yuki=#8B7DB8, koji=#C9A959, sora=#7A9E7E, karin=#B87A7A (AGENT_COLORS const)
- MissionControl.tsx is the main page component that orchestrates all sections
- `useDashboardData` hook handles API fetch with polling (5s) and JSON fallback
- Progress bars use `h-1` or `h-1.5` with `var(--bg-muted)` track and colored fill with rounded-full
- Layout.tsx uses `style={{}}` for theming, with `var()` CSS custom properties
- All transitions use `cubic-bezier(0.4, 0, 0.2, 1)` easing and 300ms duration

---

## 2026-02-11 - US-001 (new PRD v4)
- Implemented wabi-sabi theme across MissionControl.tsx and OpportunityScan.tsx
- Replaced all dark theme hardcoded colors (bg-[#111111], text-white, border-white/[0.06], text-cyan-400) with CSS custom properties
- Used inline `style={{}}` props with `var()` for all theming — keeps Tailwind for layout/spacing
- Updated type color system from Tailwind classes to CSS variable-based objects: `{ bg, color, border }`
- Updated AGENT_COLORS from Tailwind classes to hex values for use in inline styles
- index.css already had wabi-sabi variables (updated externally), Layout.tsx also pre-updated
- Files changed: src/pages/MissionControl.tsx, src/components/OpportunityScan.tsx
- **Learnings for future iterations:**
  - The theme is implemented via CSS custom properties in index.css, consumed via `style={{ color: 'var(--name)' }}`
  - getTypeColor and getTaskColor now return objects `{ bg, color, border }` instead of class strings
  - When icons need dynamic colors, add `style?: React.CSSProperties` prop alongside `className`
  - Version badge updated from v3 to v4
---

## 2026-02-11 - US-002
- Enhanced KatoQueue task cards with progress bars, agent avatars, and estimated completion time
- Extracted TaskCard into its own component with progress bar for "current" type tasks
- Added AgentAvatar component with color-coded initials (kato=cyan, yuki=violet, koji=amber, sora=emerald, karin=rose)
- Added getTaskProgress() and getEstimatedTimeLeft() helpers for time-based progress calculation
- Progress bar shows amber normally, rose when overdue; time estimate shows "~Xm left" or "overdue"
- Files changed: src/components/OpportunityScan.tsx
- **Learnings for future iterations:**
  - The KatoTask interface already had `startedAt`, `estimatedComplete`, `assignedAgent` fields — just needed UI to display them
  - Stats summary (active/planned/system counts) was already implemented in the KatoQueue component
  - Task type icons were also already implemented — the story mainly needed progress bars, agent avatars, and time estimates
  - Agent color mapping uses a simple Record lookup with fallback to gray
---

## 2026-02-11 - US-001
- Made opportunity scan cards clickable with expandable detail view
- Extracted OpportunityCard into its own component for cleaner code
- Added ChevronIcon with rotation animation for expand/collapse state
- Expanded view shows discoveredAt and expiresAt timestamps
- Files changed: src/components/OpportunityScan.tsx
- **Learnings for future iterations:**
  - The original `.map()` with block body `{}` inside JSX ternary caused TS parser issues — extracting to a separate component is cleaner
  - Icons are all inline SVGs at the bottom of the file, follow the `{ className?: string }` prop pattern
  - The component already had all type icons, priority colors, and empty state — the main missing piece was interactivity (clickable cards)
---

## 2026-02-11 - US-002 (Enhanced Opportunity Scanner)
- Enhanced OpportunityScan with P0/ROADMAP sorting, source badges, archive toggle, and conversion tracking
- Added new opportunity types: `idea` (lightbulb icon, purple) and `system` (gear icon, green)
- Added `source` badge display (ROADMAP, Project, External, System) with color coding
- Implemented sorting: P0/ROADMAP items always at top, then blockers, then by priority
- Added conversion metrics bar showing acted/dismissed/active counts
- Added Active/Archive tab toggle — dismissed/acted items move to archive view
- Added "Act on it" and "Later" action buttons in expanded card view
- Updated Opportunity interface in both component and useDashboardData hook
- Added IdeaIcon, SystemTaskIcon, DismissIcon SVG components
- Files changed: src/components/OpportunityScan.tsx, src/hooks/useDashboardData.ts
- **Learnings for future iterations:**
  - The dashboard-data.json already had `source` and `category` fields on opportunities — the component just wasn't rendering them
  - `metrics` object on OpportunityScan data has conversionRate, totalSeen, totalConverted, totalIgnored, currentActive
  - Local state for act/dismiss is in-memory only; future stories should persist to backend API
  - Twitter/Reddit placeholders are handled by the `source: 'External'` category — actual API integration is deferred
---
